<div ng-controller="dashboardController">
    <div gridster>
        <ul>
            <li gridster-item="item" ng-repeat="item in defaultItems">
                <widget></widget>
                \{{item.name}}
            </li>
        </ul>
    </div>
</div>
